# ── Ignore everything by default ──
*

# ── Allow only .cpp files ──
!*.cpp

# ── (Optional) Allow headers too ──
!*.h
!*.hpp

# ── Still ignore compiled files ──
*.exe
*.out
*.o
*.obj

# ── Ignore build/config folders ──
.vscode/
build/
*.log
